<div class="jumbotron text-center" >
  <h1 class="display-3">News Scraper</h1>
  <p class="lead"></p>
  <hr class="my-4">
  <p>Scrape the articles of said site and populate them here</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/scrape" role="button">Scrape News</a>
  </p>
</div>

<nav class="nav nav-tabs justify-content-center nav-fill" id="myTab" role="tablist">
  <a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-expanded="true">Scraped Articles</a>
  <a class="nav-item nav-link " id="nav-profile-tab" data-toggle="tab" href="#nav-profile" role="tab" aria-controls="nav-profile">Saved Articles</a>
</nav>
<div class="tab-content" id="nav-tabContent">
  <div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
    <br><br><br>
    <div class="jumbotron">
      <h1>Articles</h1>
    </div>
    <br><br>
    {{#each articles}}
    {{!-- decide later whether or not to use forms to delete or ajax --}}
      {{!-- <form action="/api/articles/delete/{{_id}}" method="POST"> --}}
        <div class="container" id ="titleDiv">
            <a href={{link}} target="_blank">{{title}}</a> <button data-id={{_id}} id="delete">delete</button><button data-id={{_id}} data-savevalue = "true"id="save">save</button>
        </div>
      {{!-- </form> --}}
          
    {{/each}}

  </div>
  <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile-tab">
    <br><br><br>
    <div class="jumbotron">
      <h1>Saved Articles</h1>
    </div>
    <br><br>
        {{#each articles}}
          {{#if saved}}
    {{!-- decide later whether or not to use forms to delete or ajax --}}
      {{!-- <form action="/api/articles/delete/{{_id}}" method="POST"> --}}
              <div class="container" id ="titleDiv">
                <a href={{link}} target="_blank">{{title}}</a> <button data-id={{_id}} id="delete">delete</button>
              </div>
      {{!-- </form> --}}
          {{/if}}
    {{/each}}

  </div>
</div>


